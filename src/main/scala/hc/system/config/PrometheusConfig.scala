package hc.system.config

case class PrometheusConfig(
                           host: String,
                           port: Int,
                           jobName: String
                           )
